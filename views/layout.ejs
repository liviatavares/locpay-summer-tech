<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= title || 'Simulação de Operações' %></title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
      body {
        font-family: 'Montserrat', sans-serif;
        background: #e3e5ec;
        margin: 0;
        padding: 0;
        overflow-x: hidden;
      }

      header {
        background-color: #074377;
        padding: 15px 40px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        color: white;
        box-sizing: border-box;
      }

      .header-left {
        display: flex;
        align-items: center;
        gap: 15px;
      }

      .logo-img {
        height: 100px;
        width: auto;
      }

      .header-title {
        font-size: 28px;
        font-weight: 700;
      }

      .header-right .header-btn {
        font-family: 'Montserrat';
        font-size: 16px;
        padding: 8px 16px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        background-color: #80c2f4;
        color: #ffffff;
        font-weight: bold;
        transition: 0.3s;
        text-decoration: none;
      }

      .header-right .header-btn:hover {
        background-color: #1571b7;
      }

      main {
        padding: 40px;
        min-height: calc(100vh - 200px);
      }

      footer {
        font-family: 'Montserrat';
        background-color: #074377;
        padding: 30px 0;
        color: white;
        text-align: center;
        width: 100%;
        margin-top: auto;
      }
    </style>
  </head>

  <body>
    <header>
      <div class="header-left">
        <a href="/">
        <img src="/images/locpay.png" class="logo-img">
        </a>
        <span class="header-title">Simulação de Operações</span>
      </div>

      <div class="header-right">
        <a class="header-btn" href="/receivers">Recebedores</a>
        <a class="header-btn" href="/operations/create">Nova Operação</a>
      </div>
    </header>

    <main>
      <% if (typeof flash !== 'undefined' && flash) { %>
        <div class="alert alert-info"><%= flash %></div>
      <% } %>

      <%- body %>
    </main>

    <footer>
      Produzido por 
      <a href="https://www.linkedin.com/m/in/livia-saboia-tavares/" 
         target="_blank" 
         style="color: #fff; text-decoration: underline;">
        Lívia Tavares
      </a>
      (futura estagiária ;) ) para a LocPay
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
